<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>DIST-S1 on OPERA Blog</title><link>https://opera-cal-val.github.io/opera-blog/test/dist-s1/</link><description>Recent content in DIST-S1 on OPERA Blog</description><generator>Hugo -- gohugo.io</generator><language>en-US</language><copyright>Copyright © 2025, NASA JPL/Caltech</copyright><lastBuildDate>Wed, 11 Feb 2026 00:00:00 +0000</lastBuildDate><atom:link href="https://opera-cal-val.github.io/opera-blog/test/dist-s1/index.xml" rel="self" type="application/rss+xml"/><item><title>Comparing DIST-S1 and DIST-HLS over Non-Hazards</title><link>https://opera-cal-val.github.io/opera-blog/test/dist-s1/compare_dist_hls_v1/</link><pubDate>Wed, 11 Feb 2026 00:00:00 +0000</pubDate><guid>https://opera-cal-val.github.io/opera-blog/test/dist-s1/compare_dist_hls_v1/</guid><description>&lt;p&gt;This blog post qualitiatively compares the OPERA Sentinel-1 disturbance product with the OPERA DIST-HLS products across a few specific sites.
In performing this comparison, we provide a perspective on the capabilities, strengths, and limits of the Sentinel-1 product.
The DIST-HLS has two disturbance products: the generic product (&lt;code&gt;GEN-STATUS&lt;/code&gt;) and vegetation product (&lt;code&gt;VEG-STATUS&lt;/code&gt;).
The DIST-S1 is a generic change product and we refer to it as &lt;code&gt;DIST-S1&lt;/code&gt; hereafter.
The DIST-HLS &lt;code&gt;VEG-STATUS&lt;/code&gt; first models &amp;ldquo;vegetation cover&amp;rdquo; and then delineates changes when a pixel undergoes &lt;em&gt;vegetation cover loss&lt;/em&gt;.
We highlight that vegetation &lt;em&gt;gain&lt;/em&gt; is not considered.
The DIST-HLS &lt;code&gt;GEN-STATUS&lt;/code&gt; is the &lt;em&gt;statistical&lt;/em&gt; deviation of the recent acquisition with respect to the baseline of the images capturing &lt;em&gt;generic&lt;/em&gt; change.
While both DIST-HLS disturbance products are validated carefully, there are no formal requirements for the &lt;code&gt;GEN-STATUS&lt;/code&gt;.
The &lt;code&gt;DIST-S1&lt;/code&gt; product delineates disturbances based on &lt;em&gt;statistical&lt;/em&gt; variations in a very similar fashion to DIST-HLS&amp;rsquo;s &lt;code&gt;GEN-STATUS&lt;/code&gt;.
When comparing the above products (&lt;code&gt;DIST-S1&lt;/code&gt;, &lt;code&gt;VEG-STATUS&lt;/code&gt;, &lt;code&gt;GEN-STATUS&lt;/code&gt;), we share all three product layers.&lt;/p&gt;</description><content:encoded><![CDATA[<p>This blog post qualitiatively compares the OPERA Sentinel-1 disturbance product with the OPERA DIST-HLS products across a few specific sites.
In performing this comparison, we provide a perspective on the capabilities, strengths, and limits of the Sentinel-1 product.
The DIST-HLS has two disturbance products: the generic product (<code>GEN-STATUS</code>) and vegetation product (<code>VEG-STATUS</code>).
The DIST-S1 is a generic change product and we refer to it as <code>DIST-S1</code> hereafter.
The DIST-HLS <code>VEG-STATUS</code> first models &ldquo;vegetation cover&rdquo; and then delineates changes when a pixel undergoes <em>vegetation cover loss</em>.
We highlight that vegetation <em>gain</em> is not considered.
The DIST-HLS <code>GEN-STATUS</code> is the <em>statistical</em> deviation of the recent acquisition with respect to the baseline of the images capturing <em>generic</em> change.
While both DIST-HLS disturbance products are validated carefully, there are no formal requirements for the <code>GEN-STATUS</code>.
The <code>DIST-S1</code> product delineates disturbances based on <em>statistical</em> variations in a very similar fashion to DIST-HLS&rsquo;s <code>GEN-STATUS</code>.
When comparing the above products (<code>DIST-S1</code>, <code>VEG-STATUS</code>, <code>GEN-STATUS</code>), we share all three product layers.</p>
<h1 id="important-thematic-differences-betwee-dist-s1-and-dist-hls">Important Thematic Differences betwee DIST-S1 and DIST-HLS</h1>
<p>Before jumping in to the interactive maps, we want to highlight a few items that will be prevelant across all comparisons.</p>
<ul>
<li>Statistical measures of disturbance (i.e. <code>DIST-S1</code> and <code>GEN-STATUS</code>) will see any deviation from the mean as a disturbance. <em>Vegetation loss</em>, however, measures a specific type of disturbance. For example, generic changes should capture vegetation <em>growth</em> or structural changes within a cleared area. However, vegetation loss delineations (e.g. <code>VEG-STATUS</code>) will not delineate these disturbances.</li>
<li>The High/Low alerts for statistical measures of disturbance (i.e. <code>DIST-S1</code> and <code>GEN-STATUS</code>) indicate the likelihood of disturbance. That is to say, a &ldquo;high confidence&rdquo; alert is much less likely than a &ldquo;low confidence&rdquo; alert, where we are analyzing these probabilities using the baseline images. Reframing using the complement, a &ldquo;high confidence&rdquo; alert is more likely to be an anomaly than a &ldquo;low confidence&rdquo; alert. For the <code>VEG-STATUS</code>, there is a thematic view of &ldquo;high&rdquo; and &ldquo;low&rdquo;: &ldquo;High&rdquo; indicates over &gt;50% vegetation cover loss in a pixel and low indicates  &lt;50%. In particular, statistical &ldquo;high&rdquo;/&ldquo;low&rdquo; have different meainings than those in <code>VEG-STATUS</code>. For example, a disturbance that removes &lt;50% vegetation cover could be viewed as a highly unlikely with respect to a statistical measure.</li>
<li>The <code>DIST-S1</code> product uses a transformer model to estimate the mean and standard deviation of the set of baseline imagery. At a high level, the DIST-S1 model is accounting for the temporal ordering of the baseline imagery and therefore more recent images are more likely to contribute to accurate parameter estimates than those images acquired further in the past. The <code>VEG-STATUS</code> estimates the baseline vegetation cover by compositing all of the baseline images together and estimating the vegetation cover from the composite. Similarly, the <code>GEN-STATUS</code> estimates per-pixel normal parameters via standard sample estimation. In particular, the DIST-HLS products do not consider temporal ordering.</li>
<li>While the disturbance delineations may align in the different scenarios, the acquisition mode of sidelooking SAR fundamentally contrasts the down-looking optical data and a disturbance area in SAR can appear to be &ldquo;smoothed&rdquo; compared to HLS.</li>
<li>DIST-HLS has a sampling rate of 2-3 days whereas DIST-S1 has a sampling rate of about 1 week. However, Sentinel-1 does not regularly distribute all the data that it can, except over Europe and parts of North America. That said, there are instances of high latitude where scenes are too dark for HLS to distribute. Additionally, when comparing and confirming disturbances delineations from Sentinel-1 over time via different acquisition geometries (i.e. ascending and descending passes), it is harder to confirm (i.e. verify over time) disturbances particularly if disturbances are visible in one acquisition geometry and not the other.</li>
<li>If there is alignment for a particular disturbance, HLS is able to detect &ldquo;finer&rdquo; features than SAR, i.e. features that are 1-2 pixels in width like a clearing for a road or path in a forest.</li>
<li>SAR is sensitive to structure as opposed to spectral changes (like &ldquo;green&rdquo;-ness). For example, sparse green vegetation that is cleared will <em>not</em> appear as disturbance in SAR and steep inclines will sometimes prevent accurate delineations from being identified on such terrain.</li>
<li>SAR is sensitive to soil moisture, rain, snow, and floating vegetation/sediment. If these appear in a recent acquisition and represent deviations from the baseline, these will show up as provisional changes. If they are transient disturbances, these delineations will be filtered out over time.</li>
<li>The confirmation process provides a means to &ldquo;filter&rdquo; out changes that are not consistently observed in time. However, this process all allows for resets of changes if a change is &ldquo;confirmed&rdquo;, then returns to it&rsquo;s baseline as &ldquo;finished&rdquo;, and then new changes are detected. If this cycle occurs, then changes that were confirmed are not accumulated. For <code>DIST-S1</code>, where regrowth, urban development, and moisture sensitivities are detected, there can appear changes that are confirmed but not carried over in the most recent <code>DIST-S1</code> product.</li>
</ul>
<p><a href="https://geog.umd.edu/facultyprofile/pickens/amy">Dr. Amy Pickens</a> selected mant of the sites below for informal comparisons except those related to a specific hazard. Most of the data represents changes detected in products generated on or before August 1st, 2025. We used 4 months of data products prior to a given date to confirm confirm the changes in the products shown.</p>
<h1 id="examples">Examples</h1>
<h2 id="logging">Logging</h2>
<h3 id="central-sweeden">Central Sweeden</h3>
<p>Below is a portion of a managed forest in Gävleborg County, Sweeden.</p>





<link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" />
<script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>
<script src="https://unpkg.com/pmtiles@3.0.7/dist/pmtiles.js"></script>


<div id="sweden-map" data-base="https://opera-cal-val.github.io/opera-blog/test/" style="height: 600px; width: 100%; margin: 20px 0;"></div>

<script>
(async () => {
  
  
  const pathPrefix = new URL(document.getElementById('sweden-map').dataset.base).pathname.replace(/\/$/, '');
  const map = L.map('sweden-map');
  

  const esri = L.tileLayer('https://server.arcgisonline.com/ArcGIS/rest/services/World_Imagery/MapServer/tile/{z}/{y}/{x}', {
    attribution: 'Tiles &copy; Esri', maxZoom: 19
  }).addTo(map);

  const google = L.tileLayer('https://mt{s}.google.com/vt/lyrs=s&x={x}&y={y}&z={z}', {
    attribution: 'Imagery &copy; Google', maxZoom: 21, subdomains: ['0', '1', '2', '3']
  });

  const osm = L.tileLayer('https://{s}.basemaps.cartocdn.com/rastertiles/voyager/{z}/{x}/{y}{r}.png', {
    attribution: '© OpenStreetMap contributors © CARTO', maxZoom: 19,
    subdomains: 'abcd'
  });

  
  function makePMTilesLayer(p) {
    return L.GridLayer.extend({
      createTile(coords, done) {
        const tile = document.createElement('img');
        p.getZxy(coords.z, coords.x, coords.y).then(data => {
          if (data) {
            const blobUrl = URL.createObjectURL(new Blob([data.data], {type: 'image/png'}));
            tile.src = blobUrl;
            tile.onload = () => { URL.revokeObjectURL(blobUrl); done(null, tile); };
            tile.onerror = () => done(new Error('tile error'), tile);
          } else { done(new Error('no data'), tile); }
        }).catch(err => done(err, tile));
        return tile;
      }
    });
  }

  const layers = JSON.parse("\n[ \n  {\"url\": \"/map_data/compare_dist_hls_v1/sweeden_logging_example/OPERA_L3_DIST-ALERT-S1_T33VWJ_20250729T162213Z_20251005T203603Z_S1A_30_v0.1_GEN-DIST-STATUS_subset.pmtiles\", \"label\": \"DIST-S1 (2025-07-29)\"},\n  {\"url\": \"/map_data/compare_dist_hls_v1/sweeden_logging_example/OPERA_L3_DIST-ALERT-HLS_T33VWJ_20250801T104041Z_20250814T031730Z_S2A_30_v1_VEG-DIST-STATUS_subset.pmtiles\", \"label\": \"DIST-HLS Vegetation (2025-08-01)\"},\n  {\"url\": \"/map_data/compare_dist_hls_v1/sweeden_logging_example/OPERA_L3_DIST-ALERT-HLS_T33VWJ_20250801T104041Z_20250814T031730Z_S2A_30_v1_GEN-DIST-STATUS_subset.pmtiles\", \"label\": \"DIST-HLS Generic (2025-08-01)\"}\n]\n");
  const overlays = {};
  let centered = false;

  for (const [i, layer] of layers.entries()) {
    const p = new pmtiles.PMTiles(`${pathPrefix}${layer.url}`);
    const LayerClass = makePMTilesLayer(p);
    const leafletLayer = new LayerClass({opacity: 0.8, attribution: 'JPL/Caltech', maxZoom: 16, minZoom: 0});
    overlays[layer.label] = leafletLayer;

    
    if (i === 0) {
      leafletLayer.addTo(map);
      if (!centered) {
        centered = true;
        p.getHeader().then(h => map.fitBounds([[h.minLat, h.minLon], [h.maxLat, h.maxLon]]));
      }
    }
  }

  L.control.layers({"ESRI Imagery": esri, "Google Satellite": google, "OpenStreetMap": osm}, overlays).addTo(map);
})();
</script>

<p>Things to note:</p>
<ul>
<li>There is a lot of alignment between <code>VEG-STATUS</code> and <code>DIST-S1</code>. We note that the large &ldquo;high&rdquo; confidence alerts in <code>DIST-S1</code> are &ldquo;low&rdquo; alerts in <code>VEG-STATUS</code>. This means that this was &ldquo;unlikely&rdquo; with respect to the baseline collected from S1, but the modeled vegetation cover within said pixels is &lt;50%.</li>
<li>The larger disturbances delineated in <code>DIST-S1</code> not present in <code>VEG-STATUS</code> appear over bare areas with respect to the base satellite imagery. We speculate such areas corresponds to recent vegetation growth that is by definition not in <code>VEG-STATUS</code>.</li>
<li>The DIST-HLS products contain some finer features like trimming forestands at the edges.</li>
</ul>
<h2 id="mining">Mining</h2>
<h3 id="indonesia">Indonesia</h3>
<p>Below is a nickel mine on Weda Bay in Indonesia.</p>





<div id="indonesia-map" data-base="https://opera-cal-val.github.io/opera-blog/test/" style="height: 600px; width: 100%; margin: 20px 0;"></div>

<script>
(async () => {
  
  
  const pathPrefix = new URL(document.getElementById('indonesia-map').dataset.base).pathname.replace(/\/$/, '');
  const map = L.map('indonesia-map');
  

  const esri = L.tileLayer('https://server.arcgisonline.com/ArcGIS/rest/services/World_Imagery/MapServer/tile/{z}/{y}/{x}', {
    attribution: 'Tiles &copy; Esri', maxZoom: 19
  }).addTo(map);

  const google = L.tileLayer('https://mt{s}.google.com/vt/lyrs=s&x={x}&y={y}&z={z}', {
    attribution: 'Imagery &copy; Google', maxZoom: 21, subdomains: ['0', '1', '2', '3']
  });

  const osm = L.tileLayer('https://{s}.basemaps.cartocdn.com/rastertiles/voyager/{z}/{x}/{y}{r}.png', {
    attribution: '© OpenStreetMap contributors © CARTO', maxZoom: 19,
    subdomains: 'abcd'
  });

  
  function makePMTilesLayer(p) {
    return L.GridLayer.extend({
      createTile(coords, done) {
        const tile = document.createElement('img');
        p.getZxy(coords.z, coords.x, coords.y).then(data => {
          if (data) {
            const blobUrl = URL.createObjectURL(new Blob([data.data], {type: 'image/png'}));
            tile.src = blobUrl;
            tile.onload = () => { URL.revokeObjectURL(blobUrl); done(null, tile); };
            tile.onerror = () => done(new Error('tile error'), tile);
          } else { done(new Error('no data'), tile); }
        }).catch(err => done(err, tile));
        return tile;
      }
    });
  }

  const layers = JSON.parse("\n[\n  {\"url\": \"/map_data/compare_dist_hls_v1/indonesia_mining_example/OPERA_L3_DIST-ALERT-S1_T52NCF_20250728T210950Z_20260205T183920Z_S1A_30_v0.1_GEN-DIST-STATUS_subset.pmtiles\", \"label\": \"DIST-S1 (2025-07-28)\"},\n  {\"url\": \"/map_data/compare_dist_hls_v1/indonesia_mining_example/OPERA_L3_DIST-ALERT-HLS_T52NCF_20250730T015641Z_20250815T152112Z_S2A_30_v1_VEG-DIST-STATUS_subset.pmtiles\", \"label\": \"DIST-HLS Vegetation (2025-07-30)\"},\n  {\"url\": \"/map_data/compare_dist_hls_v1/indonesia_mining_example/OPERA_L3_DIST-ALERT-HLS_T52NCF_20250730T015641Z_20250815T152112Z_S2A_30_v1_GEN-DIST-STATUS_subset.pmtiles\", \"label\": \"DIST-HLS Generic (2025-07-30)\"}\n]\n");
  const overlays = {};
  let centered = false;

  for (const [i, layer] of layers.entries()) {
    const p = new pmtiles.PMTiles(`${pathPrefix}${layer.url}`);
    const LayerClass = makePMTilesLayer(p);
    const leafletLayer = new LayerClass({opacity: 0.8, attribution: 'JPL/Caltech', maxZoom: 16, minZoom: 0});
    overlays[layer.label] = leafletLayer;

    
    if (i === 0) {
      leafletLayer.addTo(map);
      if (!centered) {
        centered = true;
        p.getHeader().then(h => map.fitBounds([[h.minLat, h.minLon], [h.maxLat, h.maxLon]]));
      }
    }
  }

  L.control.layers({"ESRI Imagery": esri, "Google Satellite": google, "OpenStreetMap": osm}, overlays).addTo(map);
})();
</script>

<p>Things to note:</p>
<ul>
<li>The process of mining in this area undergoes the cycle: vegetation clearing and then structural build-up. We note that statistical/generic measures will detect both, but vegetation loss delineations (<code>VEG-STATUS</code>) will only detect the first.</li>
<li>We can see build up of various mining areas switching between the basemaps (ESRI and Google Satellite).</li>
</ul>
<h2 id="development-and-road-expansion">Development and Road Expansion</h2>
<h3 id="northeastern-china">Northeastern China</h3>
<p>This scene is capturing some of the recent development that is going on throughout China on a regular basis.
This scene shows the conversion of green areas to buildings with solar paneled roofs as well as road expansion.</p>





<div id="china-map" data-base="https://opera-cal-val.github.io/opera-blog/test/" style="height: 600px; width: 100%; margin: 20px 0;"></div>

<script>
(async () => {
  
  
  const pathPrefix = new URL(document.getElementById('china-map').dataset.base).pathname.replace(/\/$/, '');
  const map = L.map('china-map');
  

  const esri = L.tileLayer('https://server.arcgisonline.com/ArcGIS/rest/services/World_Imagery/MapServer/tile/{z}/{y}/{x}', {
    attribution: 'Tiles &copy; Esri', maxZoom: 19
  }).addTo(map);

  const google = L.tileLayer('https://mt{s}.google.com/vt/lyrs=s&x={x}&y={y}&z={z}', {
    attribution: 'Imagery &copy; Google', maxZoom: 21, subdomains: ['0', '1', '2', '3']
  });

  const osm = L.tileLayer('https://{s}.basemaps.cartocdn.com/rastertiles/voyager/{z}/{x}/{y}{r}.png', {
    attribution: '© OpenStreetMap contributors © CARTO', maxZoom: 19,
    subdomains: 'abcd'
  });

  
  function makePMTilesLayer(p) {
    return L.GridLayer.extend({
      createTile(coords, done) {
        const tile = document.createElement('img');
        p.getZxy(coords.z, coords.x, coords.y).then(data => {
          if (data) {
            const blobUrl = URL.createObjectURL(new Blob([data.data], {type: 'image/png'}));
            tile.src = blobUrl;
            tile.onload = () => { URL.revokeObjectURL(blobUrl); done(null, tile); };
            tile.onerror = () => done(new Error('tile error'), tile);
          } else { done(new Error('no data'), tile); }
        }).catch(err => done(err, tile));
        return tile;
      }
    });
  }

  const layers = JSON.parse("\n[\n  {\"url\": \"/map_data/compare_dist_hls_v1/china_road_expansion_example/OPERA_L3_DIST-ALERT-S1_T50SQE_20250720T100431Z_20260205T184044Z_S1A_30_v0.1_GEN-DIST-STATUS_subset.pmtiles\", \"label\": \"DIST-S1 (2025-07-20)\"},\n  {\"url\": \"/map_data/compare_dist_hls_v1/china_road_expansion_example/OPERA_L3_DIST-ALERT-HLS_T50SQE_20250729T024611Z_20250815T053457Z_S2C_30_v1_VEG-DIST-STATUS_subset.pmtiles\", \"label\": \"DIST-HLS Vegetation (2025-07-29)\"},\n  {\"url\": \"/map_data/compare_dist_hls_v1/china_road_expansion_example/OPERA_L3_DIST-ALERT-HLS_T50SQE_20250729T024611Z_20250815T053457Z_S2C_30_v1_GEN-DIST-STATUS_subset.pmtiles\", \"label\": \"DIST-HLS Generic (2025-07-29)\"}\n]\n");
  const overlays = {};
  let centered = false;

  for (const [i, layer] of layers.entries()) {
    const p = new pmtiles.PMTiles(`${pathPrefix}${layer.url}`);
    const LayerClass = makePMTilesLayer(p);
    const leafletLayer = new LayerClass({opacity: 0.8, attribution: 'JPL/Caltech', maxZoom: 16, minZoom: 0});
    overlays[layer.label] = leafletLayer;

    
    if (i === 0) {
      leafletLayer.addTo(map);
      if (!centered) {
        centered = true;
        p.getHeader().then(h => map.fitBounds([[h.minLat, h.minLon], [h.maxLat, h.maxLon]]));
      }
    }
  }

  L.control.layers({"ESRI Imagery": esri, "Google Satellite": google, "OpenStreetMap": osm}, overlays).addTo(map);
})();
</script>

<p>Things to note:</p>
<ul>
<li>you can see some of the development at high resolution looking at the Google Maps and then switching to ESRI Imagery</li>
<li>The DIST-HLS product shows the creation of a narrow road or railway being built. However, it seems like this is ongoing and the changes are not confirmed in either <code>VEG-STATUS</code> nor <code>GEN-STATUS</code>.</li>
<li>The <code>DIST-S1</code> product is very good at seeing the construction of buildings with solar panelled roofs.</li>
</ul>
]]></content:encoded></item><item><title>The Los Angeles Fires 2025</title><link>https://opera-cal-val.github.io/opera-blog/test/dist-s1/los_angeles_fires/</link><pubDate>Sun, 02 Nov 2025 00:00:00 +0000</pubDate><guid>https://opera-cal-val.github.io/opera-blog/test/dist-s1/los_angeles_fires/</guid><description>&lt;p&gt;&lt;img src="https://upload.wikimedia.org/wikipedia/commons/1/1a/PalisadesFire_fromDowntown.png" alt="Toastt21"&gt; Image attribution &lt;a href="https://creativecommons.org/licenses/by-sa/4.0"&gt;CC BY-SA 4.0&lt;/a&gt;, via Wikimedia Commons&lt;/p&gt;
&lt;p&gt;The Los Angeles wildfires in January 2025 devastated our city including many at JPL.
It&amp;rsquo;s a hazard that many on the OPERA team were personally impacted.
Due to its felt proximity, we were eager to see how well the DIST-S1 product fared.
The OPERA DIST-S1 product provides disturbance alerts and accumulates changes over time.
It is far from perfect, but it is able to provide all-weather, all-illumination observations.
We will see how the burn areas fared against hand drawn maps.
We will also see how the changes accumulated evolved over time.&lt;/p&gt;</description><content:encoded><![CDATA[<p><img src="https://upload.wikimedia.org/wikipedia/commons/1/1a/PalisadesFire_fromDowntown.png" alt="Toastt21"> Image attribution <a href="https://creativecommons.org/licenses/by-sa/4.0">CC BY-SA 4.0</a>, via Wikimedia Commons</p>
<p>The Los Angeles wildfires in January 2025 devastated our city including many at JPL.
It&rsquo;s a hazard that many on the OPERA team were personally impacted.
Due to its felt proximity, we were eager to see how well the DIST-S1 product fared.
The OPERA DIST-S1 product provides disturbance alerts and accumulates changes over time.
It is far from perfect, but it is able to provide all-weather, all-illumination observations.
We will see how the burn areas fared against hand drawn maps.
We will also see how the changes accumulated evolved over time.</p>
<h2 id="status-layer">Status Layer</h2>
<p>Let&rsquo;s first examine what the product looks like 2 months after the fire.
The most representative layer is the <code>DIST-GEN-STATUS</code> layer, which represents the disturbance delineations detected by our product.
A description of this layer, its labels, and color map can be found <a href="https://opera-adt.github.io/dist-s1/test/pages/product_documentation/disturbance_labels/">here</a>:</p>
<p><img src="/opera-blog/test/dist-s1/los_angeles_fires/dist-labels.png" alt="dist_labels"></p>
<p>We overlay the perimeters extracted from the <a href="https://data-nifc.opendata.arcgis.com/datasets/nifc::wfigs-current-interagency-fire-perimeters/about">Wildland Fire Interagency Geospatial Services</a>, a massive database of burn perimeters within the United States.
To get all the relevant perimeters (Eaton, Palisades, Kenneth, Sunset), we examined +/- 20 days from January 4th, 2025 within MGRS tile 11SLT.
Again, the status layer shown is actually 2 months after the event.
This allows us to see the full burn area and the accumulated changes detected by the product.</p>


<link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" />
<script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>
<script src="https://unpkg.com/pmtiles@3.0.7/dist/pmtiles.js"></script>

<div id="fire-map" style="height: 600px; width: 100%; margin: 20px 0;"></div>

<script>
(async () => {
  // Initialize map centered on Los Angeles
  const map = L.map('fire-map').setView([33.85, -118.75], 9);

  // Add base ESRI imagery tiles
  const esriImagery = L.tileLayer('https://server.arcgisonline.com/ArcGIS/rest/services/World_Imagery/MapServer/tile/{z}/{y}/{x}', {
    attribution: 'Tiles &copy; Esri &mdash; Source: Esri, i-cubed, USDA, USGS, AEX, GeoEye, Getmapping, Aerogrid, IGN, IGP, UPR-EGP, and the GIS User Community',
    maxZoom: 19
  }).addTo(map);

  // Add OpenStreetMap as alternative basemap
  const osmLayer = L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
    attribution: '© OpenStreetMap contributors',
    maxZoom: 19
  });

  // Load PMTiles - detect path based on current location
  const pathPrefix = window.location.pathname.startsWith('/opera-blog') ? '/opera-blog' : '';
  const pmtilesUrl = `${pathPrefix}/map_data/los_angeles/los_angeles_2025.pmtiles`;
  const p = new pmtiles.PMTiles(pmtilesUrl);

  // Debug: Check if PMTiles loads
  p.getHeader().then(h => {
    console.log('PMTiles header:', h);
  }).catch(e => {
    console.error('Error loading PMTiles:', e);
  });

  // Custom GridLayer for PMTiles raster
  const PMTilesLayer = L.GridLayer.extend({
    createTile: function(coords, done) {
      const tile = document.createElement('img');

      // Fetch tile from PMTiles
      p.getZxy(coords.z, coords.x, coords.y).then(data => {
        if (data) {
          const blob = new Blob([data.data], { type: 'image/png' });
          const url = URL.createObjectURL(blob);
          tile.src = url;

          // Clean up blob URL after image loads
          tile.onload = () => {
            URL.revokeObjectURL(url);
            done(null, tile);
          };
          tile.onerror = () => {
            done(new Error('Tile load error'), tile);
          };
        } else {
          done(new Error('No tile data'), tile);
        }
      }).catch(err => {
        console.error('Error fetching tile:', err);
        done(err, tile);
      });

      return tile;
    }
  });

  // Add PMTiles layer to map
  const pmtilesLayer = new PMTilesLayer({
    opacity: 0.8,
    attribution: 'JPL/Caltech',
    maxZoom: 16,
    minZoom: 0
  });

  pmtilesLayer.addTo(map);

  // Load and add GeoJSON layer
  const geojsonResponse = await fetch(`${pathPrefix}/map_data/los_angeles/los_angeles_fires.geojson`);
  const geojsonData = await geojsonResponse.json();

  const geojsonLayer = L.geoJSON(geojsonData, {
    style: {
      color: '#ff0000',
      weight: 2,
      opacity: 0.8,
      fillOpacity: 0.3
    },
    onEachFeature: function(feature, layer) {
      if (feature.properties) {
        let popupContent = '<div>';
        for (const [key, value] of Object.entries(feature.properties)) {
          popupContent += `<strong>${key}:</strong> ${value}<br>`;
        }
        popupContent += '</div>';
        layer.bindPopup(popupContent);
      }
    }
  }).addTo(map);

  // Add layer control with clickable basemaps
  const baseLayers = {
    "ESRI Imagery": esriImagery,
    "OpenStreetMap": osmLayer
  };

  const overlays = {
    "DIST-S1 Status (2025-03-03)": pmtilesLayer,
    "Fire Perimeters": geojsonLayer
  };

  L.control.layers(baseLayers, overlays).addTo(map);
})();
</script>

<p>It&rsquo;s also helpful to see how the status layers change over time and what to expect.
Los Angeles is well represented by Sentinel-1 imaging, meaning that the available images are released by the European Space Agency (ESA) and we can process them.
In the time-series below, you often see images taken twice in 1 day because there is an ascending and descending track over LA.</p>
<p><img src="/opera-blog/test/dist-s1/los_angeles_fires/status.gif" alt="status-ts"></p>
<p>For example, SAR is highly sensitive to rain.
Los Angeles had an unusually wet February including an <a href="https://cw3e.ucsd.edu/cw3e-event-summary-12-14-february-2025/">atmospheric river</a> on
February 14th of 2025.
You can see erroneous changes associated to this that are marked as alert/provisional signal and then are removed over time with confirmation process.</p>
<h2 id="eaton-fire-zoom-in">Eaton Fire Zoom In</h2>
<p>Let&rsquo;s take a closer look at the Eaton Fire with additional external data layers including damage indicators as well as the DIST-S1 metric.
The <strong>D</strong>amage <strong>Ins</strong>pection (DINS) data from CalFire (here are the links for the <a href="https://hub-calfire-forestry.hub.arcgis.com/datasets/cal-fire-damage-inspection-dins-data/explore">complete CA DINS database</a> and just for <a href="https://gis.data.ca.gov/datasets/CALFIRE-Forestry::dins-2025-eaton-public-view/about">Eaton</a>).
One thing to note is that the status layer confirms/accumulates changes through all viewing geometries and over time.
Our confirmed layers do not capture the entire burn perimeter.
However, if we look at the metric (coming from January 21st), we can see good visual alignment with the DINS data.
The metric is a way to quantitatively measure how disturbed a pixel is.
Higher values indicate higher statistical likelihood of disturbance and lower, less.
Visually, we scaled the metric to be between 2 and 5, so that values below 2 are set to the visual min (blue) and values above 5 are set to the visual max (yellow).</p>


<div id="eaton-fire-map" style="height: 600px; width: 100%; margin: 20px 0;"></div>

<script>
(async () => {
  // Initialize map centered on Eaton Fire area
  const eatonMap = L.map('eaton-fire-map').setView([34.18, -118.08], 12);

  // Add base ESRI imagery tiles
  const esriImagery = L.tileLayer('https://server.arcgisonline.com/ArcGIS/rest/services/World_Imagery/MapServer/tile/{z}/{y}/{x}', {
    attribution: 'Tiles &copy; Esri &mdash; Source: Esri, i-cubed, USDA, USGS, AEX, GeoEye, Getmapping, Aerogrid, IGN, IGP, UPR-EGP, and the GIS User Community',
    maxZoom: 19
  }).addTo(eatonMap);

  // Add OpenStreetMap as alternative basemap
  const osmLayer = L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
    attribution: '© OpenStreetMap contributors',
    maxZoom: 19
  });

  // Load PMTiles - detect path based on current location
  const pathPrefix = window.location.pathname.startsWith('/opera-blog') ? '/opera-blog' : '';

  // Load Eaton Fire Status PMTiles
  const eatonStatusUrl = `${pathPrefix}/map_data/los_angeles/eaton_fire_2025_status.pmtiles`;
  const eatonStatusPMTiles = new pmtiles.PMTiles(eatonStatusUrl);

  // Load Metric PMTiles
  const metricUrl = `${pathPrefix}/map_data/los_angeles/metric_20250121_subset_eaton.pmtiles`;
  const metricPMTiles = new pmtiles.PMTiles(metricUrl);

  // Custom GridLayer for PMTiles raster
  const PMTilesLayer = L.GridLayer.extend({
    initialize: function(pmtilesInstance, options) {
      L.GridLayer.prototype.initialize.call(this, options);
      this.pmtiles = pmtilesInstance;
    },
    createTile: function(coords, done) {
      const tile = document.createElement('img');

      // Fetch tile from PMTiles
      this.pmtiles.getZxy(coords.z, coords.x, coords.y).then(data => {
        if (data) {
          const blob = new Blob([data.data], { type: 'image/png' });
          const url = URL.createObjectURL(blob);
          tile.src = url;

          // Clean up blob URL after image loads
          tile.onload = () => {
            URL.revokeObjectURL(url);
            done(null, tile);
          };
          tile.onerror = () => {
            done(new Error('Tile load error'), tile);
          };
        } else {
          done(new Error('No tile data'), tile);
        }
      }).catch(err => {
        console.error('Error fetching tile:', err);
        done(err, tile);
      });

      return tile;
    }
  });

  // Add Eaton Status PMTiles layer
  const eatonStatusLayer = new PMTilesLayer(eatonStatusPMTiles, {
    opacity: 0.8,
    attribution: 'JPL/Caltech',
    maxZoom: 16,
    minZoom: 0
  });

  eatonStatusLayer.addTo(eatonMap);

  // Add Metric PMTiles layer (initially hidden)
  const metricLayer = new PMTilesLayer(metricPMTiles, {
    opacity: 0.8,
    attribution: 'JPL/Caltech',
    maxZoom: 16,
    minZoom: 0
  });

  // Load Eaton Perimeter GeoJSON
  const eatonPerimeterResponse = await fetch(`${pathPrefix}/map_data/los_angeles/eaton_perimeter.geojson`);
  const eatonPerimeterData = await eatonPerimeterResponse.json();

  const eatonPerimeterLayer = L.geoJSON(eatonPerimeterData, {
    style: {
      color: '#ff0000',
      weight: 2,
      opacity: 0.8,
      fillOpacity: 0.1
    },
    onEachFeature: function(feature, layer) {
      if (feature.properties) {
        let popupContent = '<div>';
        for (const [key, value] of Object.entries(feature.properties)) {
          popupContent += `<strong>${key}:</strong> ${value}<br>`;
        }
        popupContent += '</div>';
        layer.bindPopup(popupContent);
      }
    }
  }).addTo(eatonMap);

  // Load DINS Eaton GeoJSON (now in EPSG:4326)
  const dinsEatonResponse = await fetch(`${pathPrefix}/map_data/los_angeles/dins_eaton_formatted.geojson`);
  const dinsEatonData = await dinsEatonResponse.json();

  const dinsEatonLayer = L.geoJSON(dinsEatonData, {
    pointToLayer: function(feature, latlng) {
      // Color based on damage level
      let color = '#00ff00'; // Default green for No Damage
      const damage = feature.properties.Damage || '';
      if (damage.includes('Destroyed')) {
        color = '#ff0000'; // Red
      } else if (damage.includes('Major') || damage.includes('Minor')) {
        color = '#ff8800'; // Orange
      } else if (damage.includes('Affected')) {
        color = '#ffff00'; // Yellow
      }

      return L.circleMarker(latlng, {
        radius: 4,
        fillColor: color,
        color: '#000',
        weight: 1,
        opacity: 0.8,
        fillOpacity: 0.7
      });
    },
    onEachFeature: function(feature, layer) {
      if (feature.properties) {
        let popupContent = '<div>';
        for (const [key, value] of Object.entries(feature.properties)) {
          popupContent += `<strong>${key}:</strong> ${value}<br>`;
        }
        popupContent += '</div>';
        layer.bindPopup(popupContent);
      }
    }
  }).addTo(eatonMap);

  // Add layer control
  const baseLayers = {
    "ESRI Imagery": esriImagery,
    "OpenStreetMap": osmLayer
  };

  const overlays = {
    "Eaton Fire Status (2025-03-03)": eatonStatusLayer,
    "Metric (2025-01-21)": metricLayer,
    "Eaton Fire Perimeter": eatonPerimeterLayer,
    "Damage Indicators (DINS)": dinsEatonLayer
  };

  L.control.layers(baseLayers, overlays).addTo(eatonMap);

  // Add legend for DINS damage indicators
  const legend = L.control({ position: 'bottomright' });

  legend.onAdd = function(map) {
    const div = L.DomUtil.create('div', 'info legend');
    div.style.backgroundColor = 'white';
    div.style.padding = '10px';
    div.style.border = '2px solid rgba(0,0,0,0.2)';
    div.style.borderRadius = '5px';
    div.style.fontSize = '12px';

    const grades = [
      { label: 'Destroyed (>50%)', color: '#ff0000' },
      { label: 'Major/Minor (10-50%)', color: '#ff8800' },
      { label: 'Affected (1-9%)', color: '#ffff00' },
      { label: 'No Damage', color: '#00ff00' }
    ];

    div.innerHTML = '<strong>DINS Damage Level</strong><br>';

    for (let i = 0; i < grades.length; i++) {
      div.innerHTML +=
        '<div style="display: flex; align-items: center; margin-top: 5px;">' +
        '<i style="background:' + grades[i].color + '; width: 18px; height: 18px; opacity: 0.7; border: 1px solid #000; border-radius: 50%; margin-right: 8px; flex-shrink: 0;"></i>' +
        '<span>' + grades[i].label + '</span>' +
        '</div>';
    }

    return div;
  };

  legend.addTo(eatonMap);
})();
</script>

]]></content:encoded></item></channel></rss>